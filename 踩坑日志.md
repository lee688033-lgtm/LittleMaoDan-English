# 踩坑日志

- 2026-09-22：按用户要求整理公开项目 README，仅修改 `README.md`。完整核对了现有 Compose 页面、BrandBottomBar、词汇 assets、Room 数据层、Repository、真题模块、Gradle/SDK 配置和 `.gitignore`。删除截图占位与过时的“后续功能”表述，补充 Kotlin/Compose/Material 3/Room/Gradle/AGP/SDK/JDK 技术栈、781 词数据规模、项目结构、v1.0.0 和 License 待确定说明。特别明确：根目录 `词汇json文件/` 是被 Git 忽略的原始目录；公开仓库包含真题源码但不含真题 JSON，因此直接克隆后真题模块没有对应题目数据。`git diff -- README.md` 与 `git status` 确认仅 `README.md` 被修改；未执行构建，因为本次仅修改文档。

- 2026-09-22：修复 Stage 3 后 BottomBar 自定义 Icon 空白问题。根因在 Stage 2 的 `brandVector()`：路径调用使用 `fill = null`，同时未提供 `stroke`，导致 ImageVector 路径没有任何可绘制的填充或描边；Stage 3 的 `navigationBarsPadding()` 与空白无关。最小修复是在同一套路径上恢复 `SolidColor(Color.Black)` 描边，保留五个 Icon、BottomBar 布局、动画参数和 Insets 策略不变。`assembleDebug --no-daemon --max-workers=1 --console=plain` 返回 `BUILD SUCCESSFUL`；APK `app/build/outputs/apk/debug/app-debug.apk`，27,819,403 bytes（26.53 MB）。APK 已安装到真机 `PKB110/YPSCWCZ5HMX4NN9X` 并成功启动；UI dump 确认五个导航 contentDescription 和 BottomBar 安全区布局存在。Icon 实际形状/颜色仍需人工肉眼确认。

- 2026-09-22：Stage 3 Edge-to-Edge/Insets 调整完成并构建通过。根 Scaffold 改为消费 `WindowInsets.safeDrawing`，主内容直接使用根 padding；`BrandBottomBar` 仅通过 `navigationBarsPadding()` 消费底部系统导航区，避免底部被遮挡。首页、单词、背诵、拼写、我的及单词详情内部 Scaffold 设置 `contentWindowInsets = WindowInsets(0, 0, 0, 0)`，不再重复消费系统 Insets；WordBookScreen 保留 `WindowInsets.safeDrawing`，因为它是无外层页面 Scaffold 的独立页面；ExamScreens 的普通页面与阅读页面保持原有处理，阅读练习/结果继续显式关闭 Insets。未修改 `enableEdgeToEdge`/`SystemBarStyle.auto`、Theme、业务逻辑或 Stage 2 图标动画。`assembleDebug --no-daemon --max-workers=1 --console=plain` 返回 `BUILD SUCCESSFUL`，APK `app/build/outputs/apk/debug/app-debug.apk`，27,819,403 bytes（26.53 MB）。未进行真机/视觉自动确认，底部空白、遮挡、键盘和不同屏幕尺寸仍需人工确认。

- 2026-09-22：Stage 2 构建验证完成。首次完整构建失败，根因为新建 `BrandBottomBar.kt` 使用当前 Compose `PathBuilder` 不支持的 `cubicTo`/`quadraticBezierTo` API；仅将品牌 Icon 路径改为兼容的 `lineTo` 路径，未修改业务、主题或 Insets。随后 `gradlew.bat assembleDebug --no-daemon --max-workers=1 --console=plain` 返回 `BUILD SUCCESSFUL`。APK 为 `app/build/outputs/apk/debug/app-debug.apk`，大小 27,819,403 bytes（26.53 MB）。

- 2026-09-22：Stage 1 UI/UX 重构只读分析完成，未修改源码。当前 `MainActivity.kt` 已调用 `enableEdgeToEdge` 与透明 `SystemBarStyle.auto`，根 Scaffold 使用 `contentWindowInsets = WindowInsets(0, 0, 0, 0)` 并仅按底部 padding 推进主内容；但首页、单词、背诵、拼写、我的及真题页面仍各自嵌套 Scaffold，Insets 策略尚未完全统一。BottomBar 位于 `ui/components/CET6Components.kt`，仍直接使用 Material `NavigationBar`/`NavigationBarItem` 和 Home/MenuBook/School/Edit/Person 默认图标，无品牌化 Icon、选中胶囊或切换动画；全项目未发现 `AnimatedContent`/`AnimatedVisibility`/`animate*` 页面切换实现。后续 Stage 2 应优先改造 `MainActivity.kt`、`CET6Components.kt`，Stage 3 再统一页面 Scaffold/Insets，Stage 4 处理五个主页面视觉层级，Stage 5 增加平级页面 Fade + 轻微垂直位移动画。现有业务入口、Room、词库、学习/拼写/真题逻辑保持不动。

- 2026-09-19：修复背诵“学到哪停到哪”。根因是 `StudyScreen` 只有生命周期兜底保存，点击上下词和从头开始没有立即写入 Room，且保存/恢复把索引限制到 `lastIndex`，无法表示整表完成。继续复用 Room 的 `study_progress` 和现有 `progressKey`，将有效索引范围改为 `0..words.size`，每次主动操作立即保存，完成态安全渲染并支持上一个/重新开始；使用 Compose 作用域内的 `Mutex` 串行保存，避免连续操作时旧写入覆盖新写入。新增 instrumentation 断言完成索引可保存且普通/随机/单词本进度互不覆盖。`assembleDebug`、`assembleDebugAndroidTest` 和 `connectedDebugAndroidTest` 均通过；设备任务报告 0 个 JUnit tests（项目当前使用自定义 Instrumentation 入口）。

- 2026-09-18：修复状态栏与顶部留白适配。根因是 Android 16/target 35 edge-to-edge 下 MainActivity 外层 Scaffold 默认 Insets 与页面内 Scaffold 叠加，且 Activity 未统一设置状态栏图标明暗；修复 `MainActivity.kt` 使用 `enableEdgeToEdge` + `SystemBarStyle.auto`，根 Scaffold 设置 `contentWindowInsets = WindowInsets(0, 0, 0, 0)` 并仅保留底部导航 padding，修复 `WordBookScreen.kt` 使用 `WindowInsets.safeDrawing` 保护无 Scaffold 页面顶部和底部安全区，同时将该页面 ArrowBack 更新为 AutoMirrored API。未修改业务逻辑、Repository、Room、JSON、Navigation 或依赖。`assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL；真机 PKB110/Android 16 安装成功，浅色和深色启动、首页标题/内容、底部导航和单词本安全区 UI dump 正常；仅保留既有 overridePathCheck、AGP/compileSdk 与 KAPT 提示，未新增 warning。

- 2026-09-18：Stage 8 重构 `presentation/screens/MyScreen.kt` 的 UI，保留 `calculateLearningStatistics(wordIds, records.getAllRecords())` 统计来源、`WordBookRepository.getWordBookCount()` 单词本数量、`reloadKey + refreshKey` 刷新机制和现有单词本 Navigation 回调；新增学习概览、双列学习统计、原有 0～3 级掌握度、单词本入口及 Loading/Empty/Error 状态，增加深色模式 Preview。首次构建发现 `weight` 显式导入和 `TopAppBar` 实验 API 问题，改为 Compose 作用域解析并沿用项目文件级 opt-in 后修复。`assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL；仅有既有 overridePathCheck、AGP/compileSdk 兼容性和 KAPT language version 回退提示，未新增 deprecated warning。未修改 Repository、Room、JSON、Navigation、其他页面、Gradle 或依赖。

- 2026-09-18：Stage 7 重构 `presentation/screens/SpellingScreen.kt` 的 UI，保留顺序/随机、trim/ignoreCase、空输入保护、正确/错误记录、重试、上下词和进度逻辑；接入统一提示卡片、输入框、进度条、结果状态、公共按钮和 Loading/Empty/Error 组件，并增加 Preview。当前 Compose 版本的 `KeyboardOptions` 需从 `androidx.compose.foundation.text` 导入，错误导入会导致编译失败；修正后 `assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL。未修改 Repository、Room、JSON、Navigation 或其他页面。
- 2026-09-18：Stage 6 重构 `presentation/screens/StudyScreen.kt` 的 UI，保留顺序/随机、进度保存恢复、释义显隐、上下词边界和单词本逻辑；接入公共状态和按钮组件，新增进度条、模式切换、单词卡片和 Preview。模式切换继续读取对应的 Room 进度键，未改变随机恢复算法。未修改 Repository、数据库、JSON 或 Navigation；`assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL，仅保留既有 KAPT 与 AGP/compileSdk 提示。
- 2026-09-18：Stage 5 重构 `presentation/screens/WordScreen.kt`，接入 Stage 3 的 CET6SearchBar、CET6WordItem、CET6LoadingState、CET6EmptyState、CET6ErrorState，并优化单词详情卡片；搜索仍使用原 WordRepository.searchWords，未修改业务逻辑、Repository、Room 或 JSON。ArrowBack 使用 AutoMirrored API；`assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL，仅保留既有 KAPT 与 AGP/compileSdk 提示。
- 2026-09-18：Stage 4 新增 `presentation/screens/HomeScreen.kt`，使用现有 WordRepository、LearningRecordRepository、WordBookRepository 和 `calculateLearningStatistics` 展示真实总词汇、学习进度、单词本数量及学习统计；首页支持滚动、加载/错误状态、背诵/拼写/单词本入口，并通过现有 refreshKey 在返回首页时刷新。仅修改 MainActivity 的首页分支和刷新回调，未修改数据库、Repository、JSON 或其他业务页面；`assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL。新增图标使用 AutoMirrored API，未保留页面图标弃用 warning。
- 2026-09-18：Stage 3 新增 `ui/components/CET6Components.kt`，提供 PrimaryButton、SecondaryButton、CET6SearchBar、CET6WordItem、CET6StatCard、CET6SectionTitle、CET6EmptyState、CET6ErrorState、CET6LoadingState 和 CET6NavigationBar；仅在 `MainActivity` 抽离底部栏视觉组件，未修改业务页面、Navigation 路由、数据库、Repository 或 JSON。组件统一从 MaterialTheme 读取颜色/形状，支持深色主题并包含 Preview；`assembleDebug --no-daemon --max-workers=1` BUILD SUCCESSFUL。
- 2026-09-18：Stage 2 新增 `ui/theme/Color.kt`、`Type.kt`、`Theme.kt`，并仅在 `MainActivity` 将根内容包入 `CET6VocabularyTheme`；未修改页面业务、数据库、Repository 或 JSON。当前 Compose 版本的 `Shape.copy(all = Dp)` 不兼容，统一改用 `RoundedCornerShape` 后 `assembleDebug` BUILD SUCCESSFUL。构建仍有既存的 MenuBook 弃用、KAPT 语言版本回退及 AGP/compileSdk 提示。
- 2026-09-16：用户明确要求不要处理图片。本项目创建过程中跳过了 `图片` 目录，不读取、不修改其中内容。
- 2026-09-16：已确认 application ID 为 `com.example.cet6vocabulary`，当前阶段只创建 Compose + Material 3 基础页面与底部导航。
- 2026-09-16：首次构建因工作区中文路径触发 Android Gradle Plugin 路径检查，已在 `gradle.properties` 增加 `android.overridePathCheck=true`。
- 2026-09-16：第二次构建进入编译但 Gradle/D8 内存不足，已将 JVM 堆调整为 4GB；再次验证时缓存被残留 Gradle 进程锁定，因此本机最终未取得成功 Build 结果。
- 2026-09-16：第二阶段先修复 Build，再建立最小 `data/model` 数据层；未导入词汇 JSON，未修改 UI 和导航。
- 2026-09-16：Build 最终成功。根因包括中文路径检查、JVM target 不一致，以及残留 Java/Gradle 进程导致的缓存锁和 D8 内存压力；停止明确的 Gradle Java 进程、单 worker 并设置 `GRADLE_OPTS` 后通过。
- 2026-09-16：第 3 阶段按用户新设计移除 `Word.day`；自由学习模式不生成、不读取、不校验 day。
- 2026-09-16：原始 JSON 共 2 个文件、150 条、ID 1-150 连续且无重复；原始文件未修改，复制到 `app/src/main/assets/cet6/`。
- 2026-09-16：`WordRepository` 从 assets/cet6 自动枚举多个 JSON，严格校验 id/word/meaning，并支持 word/meaning 的大小写不敏感搜索。
- 2026-09-16：第 3 阶段 Debug Build 成功；未进入第 4 阶段。
- 2026-09-16：第 4 阶段单词页面最终 Build 成功；修复了 LocalContext Composable 上下文和 Material 3 实验 API 注解问题。
- 2026-09-16：第 5 阶段新增 StudyScreen，支持顺序/随机序列、释义显隐、前后边界和 Loading/Error/Empty/Success；未使用 LearningRecord，未修改 WordRepository，Debug Build 成功。
- 2026-09-16：第 6 阶段新增 SpellingScreen，接入底部拼写导航；Debug Build 验证受阻，项目无 gradlew.bat 且系统未安装 gradle 命令。
- 2026-09-16：第 6 阶段补齐 Gradle 8.10.2 Wrapper（gradlew、gradlew.bat、wrapper jar/properties）；使用 JDK 17 离线 assembleDebug 首次发现 SpellingScreen 的 Compose Button 参数解析错误，改为具名 modifier 并拆分 Row 后 BUILD SUCCESSFUL。APK 位于 app/build/outputs/apk/debug/app-debug.apk。
- 2026-09-18：第 7 阶段引入 Room 2.6.1，LearningRecord 使用独立 Entity，wordId 为主键；DAO 使用 REPLACE，Repository 所有数据库操作显式切换到 Dispatchers.IO，并用 Mutex 串行化读改写。
- 2026-09-18：拼写提交在启动协程前切换为 SUBMITTING，正确/错误结果态均不再显示检查按钮；错误只有点击“再试一次”重置后才能再次计数，空输入不写数据库。
- 2026-09-18：严格离线构建首次因本机无 Room 缓存失败，联网下载固定依赖后可离线构建；另发现 PowerShell 写入 UTF-8 BOM 会使 gradle.properties 首行 org.gradle.jvmargs 失效，去除 BOM 并将堆设为 6GB 后解决 D8 OOM。
- 2026-09-18：已在连接的 Android 真机运行 Room 持久化 Instrumentation：写入记录、关闭数据库、重新打开同名文件并读取，返回“Room close/reopen persistence verified”；同时验证相同 wordId 主键替换及 correct/wrong/mastery 累加。
- 2026-09-18：第 8 阶段新增“我的”页面；统计一次读取 WordRepository 当前词库与 LearningRecordRepository 全部记录，已学习定义为 spellCount > 0，正确率按正确次数总和 / 拼写次数总和计算，零分母返回 0。
- 2026-09-18：熟练度只读取第 7 阶段已有 mastery，不修改其规则；无 LearningRecord 的当前词库单词计入 0 级，页面明确标注为“App 统计等级”。未改数据库结构、版本或拼写逻辑。
- 2026-09-18：统计 instrumentation 覆盖实际 JSON 150 词、无记录、空词库、10 次拼写 7 对 3 错、四级 mastery，以及数据库关闭重开后重新聚合；本轮无连接设备，测试源码已编译但未重新在真机执行。
- 2026-09-18：Windows Codex apply_patch 包入口直接执行曾返回 Access denied；复制 codex.exe 到临时目录后可用。PowerShell 直接传含中文/引号的长补丁会截断或乱码，改用 Node spawnSync 参数数组可无损调用 patch runner。
- 2026-09-18：第 8 阶段按指定离线命令 assembleDebug 完成，BUILD SUCCESSFUL；APK 仍输出到 app/build/outputs/apk/debug/app-debug.apk。
- 2026-09-18：第 8 阶段完整验收在真机 YPSCWCZ5HMX4NN9X 完成：安装 Debug/Test APK，instrumentation 返回 Room close/reopen persistence verified；五个底部页面均可打开，正确/错误拼写可更新统计，强制停止并重启后统计保持，随机题目在重组后保持稳定，空输入前后统计不变，“我的”页面可滚动显示 0-3 级。
- 2026-09-18：验收实际解析 assets/cet6 下 2 个 JSON 共 150 条，ID 1-150 连续无重复，word/meaning 均非空；生产统计没有硬编码词数，Word 无 day 字段。
- 2026-09-18：本轮只做验收，未修改业务代码。发现两项未处理遗留：androidTest 统计断言硬编码当前数量 150，未来词库变化会导致测试误报；WordScreen 当前显示“未记录学习状态”，与第 8 阶段原范围“不在单词页加入学习状态文字”不一致，但不影响当前功能正确性。
- 2026-09-18：验收最终执行指定离线 assembleDebug，BUILD SUCCESSFUL；APK 路径 app/build/outputs/apk/debug/app-debug.apk，大小 16674280 bytes。未进入第 9 阶段。
- 2026-09-18：第 8 阶段收尾修复完成：LearningRecordPersistenceTest 使用 WordRepository 实际词库数量驱动所有词数断言，不再硬编码 150；WordScreen 删除“未记录学习状态”预留文字，未引入任何学习记录逻辑。
- 2026-09-18：收尾验收确认 SpellingScreen、LearningRecord、MyScreen 统计逻辑、Room 数据库结构与 version 1、JSON 词库均未修改；真机 instrumentation 返回 Room close/reopen persistence verified，退出码 0；指定 assembleDebug 再次 BUILD SUCCESSFUL，APK 为 app/build/outputs/apk/debug/app-debug.apk。未进入第 9 阶段。
- 2026-09-18：第 9 阶段新增 WordBookEntity/DAO/Repository，单词本只保存 wordId 与 addedTime，wordId 主键并使用 IGNORE 防重复；LearningRecord 未改动。
- 2026-09-18：AppDatabase 从版本 1 升级到 2，增加明确 Migration(1, 2)，只创建 word_book 表并保留 learning_records；移除破坏性迁移依赖。
- 2026-09-18：第 9 阶段接入 StudyScreen、MyScreen、WordBookScreen 和单词本背诵来源；普通词库顺序/随机与单词本顺序/随机均保留随机序列一次生成逻辑。
- 2026-09-18：Windows PowerShell 写入 Kotlin 文件时误将 `r`n 写成字面量，导致多次编译错误；已清理并重新构建。
- 2026-09-18：第 9 阶段 instrumentation 在真机 YPSCWCZ5HMX4NN9X 通过，验证 Migration 1→2、LearningRecord 保留、WordBook 去重/删除/独立持久化；真机入口为 com.example.cet6vocabulary.test/com.example.cet6vocabulary.data.PersistenceInstrumentation。
- 2026-09-18：第 9 阶段指定 assembleDebug --no-daemon --max-workers=1 --offline --console=plain BUILD SUCCESSFUL；APK 为 app/build/outputs/apk/debug/app-debug.apk。
- 2026-09-18：第 9 阶段收尾 Bug 修复：WordBookScreen 原实现虽有 onBack 参数但未渲染左上角返回按钮，且未处理系统返回；新增 IconButton(onBack) 与 BackHandler(onBack)，MainActivity 对单词本页和单词本背诵页增加一致的返回状态处理。真机确认左上角返回和系统返回回到“我的”；设备单词本为空，开始背诵链路未执行。
- 2026-09-18：新增词库接入完成。原始目录新增文件实际为 `CET6_高频词汇_151-229.json`（79 条）和 `CET6_高频词汇_230-387.json`（158 条）；两文件均为 JSON 数组，字段结构与原有词库一致。未修改原始 JSON。
- 2026-09-18：新增两个 JSON 已复制到 `app/src/main/assets/cet6/`，assets 最终包含 4 个 JSON、387 条词。全量校验 ID 1-387 连续、无重复、无非法 ID、word/meaning 无空值，151-387 与 1-150 无交叉重复；源文件与 assets 文件 SHA-256 全部一致。
- 2026-09-18：确认 WordRepository 仍自动枚举 assets/cet6 下所有 `.json`，使用 Android `JSONArray` 解析、合并、校验重复 ID 后按 ID 排序，本次未修改 WordRepository。
- 2026-09-18：确认单词页、背诵页、拼写页和“我的”页面均通过 WordRepository 动态读取词库；统计总词数使用当前词库 ID 集合，未硬编码 387。新增词不会自动创建 LearningRecord，也不会自动加入 WordBook；Room 版本和 Migration 未改。
- 2026-09-18：指定命令 `gradlew.bat assembleDebug --no-daemon --max-workers=1 --offline --console=plain` BUILD SUCCESSFUL，APK 为 `app/build/outputs/apk/debug/app-debug.apk`，大小 16693818 bytes。
- 2026-09-18：真机 YPSCWCZ5HMX4NN9X 在线，主 APK 与测试 APK 安装成功；直接运行现有 PersistenceInstrumentation 返回 `Room close/reopen persistence verified`。标准 `connectedDebugAndroidTest` 未能完成：离线缺少 UTP 插件缓存，联网重试时 Google Maven 连接超时，未进入测试执行阶段。
- 2026-09-18：本次仅扩充词库数据，未进入第 10 阶段。
- 2026-09-18：第 9.5 阶段使用 Windows `apply_patch.bat` 时遇到入口拒绝执行、长补丁参数截断和临时执行器自删除；复制 `codex.exe` 到临时路径并使用短补丁/临时补丁文件完成修改。
- 2026-09-18：StudyProgressEntity 初次补丁中的 Entity tableName 引号被补丁参数转义吞掉，导致 kapt 报 `tableName = null` 常量错误；修正源码后标准 Build 成功。
- 2026-09-18：首次真机安装命令误用了不存在的固定 SDK 路径，改用 PATH 中实际可用的 adb 后安装和 Instrumentation 均成功。
- 2026-09-18：第 9.5 阶段完成 Room 2→3 迁移、四类背诵进度持久化和真机数据库测试；Instrumentation 返回 `Room close/reopen persistence verified`。
- 2026-09-18：第 9.5 阶段 UI 真机验收时，设备拒绝 `pm clear com.example.cet6vocabulary`（无 `CLEAR_APP_USER_DATA` 权限），因此无法用清空应用数据建立完全干净的验收状态；改用现有持久化状态继续验证。
- 2026-09-18：UI 验收中途设备出现非 CET6 应用的前台浮层，UI dump 文本与 CET6 无关；通过 force-stop 并重新启动 CET6 App 恢复，受影响的后续人工 UI 项不据此判定通过。
- 2026-09-18：剩余 UI 验收中，单词本入口需要先滚动才能进入；设备 UI dump 实际按钮文本为“下一个”而非测试清单中的“下一题”，且 PowerShell 向 Node 传递中文正则时发生转码，导致部分文本定位脚本无法可靠断言；未据此虚构 PASS，业务代码未修改。
- 2026-09-19：最终补测通过真实 UI 将单词本逐个移除至 0 个，页面显示“单词本还是空的”，点击“开始背诵”未进入空背诵页。
- 2026-09-19：最终补测中设备再次被 Launcher/系统浮层抢前台，导致 LearningRecord 统计前后对比无法可靠完成；拼写页空输入保持提示，错误答案实际显示“拼写错误”和标准答案，正确答案完整判定未可靠完成。
- 2026-09-19：从 CET6 拼写页执行 Android 系统返回键后，前台实际切换到 Launcher/“自选照片”，未回到 CET6 页面；未修改导航业务代码，返回键验收按未执行处理。
- 2026-09-18：最后一轮 UI 补测中，单词本通过真实 UI 删除至 0 个并显示空状态；之后设备多次被 Launcher/“小布建议”前台覆盖，导致 LearningRecord 统计节点、拼写节点和系统返回目标页面无法可靠确认；相关项目按未执行报告，未修改业务代码。

## Stage 8.5 真机验收

### 设备
设备型号：PKB110；Android 版本：16；adb serial：YPSCWCZ5HMX4NN9X。
验收设备分辨率：1080x2374，非 360dp 窄屏设备。

### Build
assembleDebug：PASS
APK：`app/build/outputs/apk/debug/app-debug.apk`
词库核对：4 个 JSON，387 个词，ID 1-387 唯一连续。

### 页面测试
首页：UI PASS；核心功能 PASS（真实统计、滚动、底部导航入口可用）；状态 PASS。
单词：UI PASS；核心功能 PASS（387 条列表、英文 abandon 搜索、空结果、列表内容可用）；中文搜索和详情返回未完成可靠自动断言。
背诵：UI PASS；核心功能 PASS（顺序、上下词、释义切换、随机入口、单词本加入/移出可操作）；状态 PASS。
拼写：UI PASS；核心功能 PASS（随机入口、空输入保护、提交结果锁定路径可用）；正确答案输入受设备输入法/当前随机题目影响，未作为 PASS 断言。
我的：UI PASS；核心功能 PASS（动态统计、四级掌握度、滚动、单词本入口和返回可用）；状态 PASS。

### 持久化
学习记录：PASS，已有记录在验收过程中显示并由拼写页面更新统计。
单词本：PASS，加入后显示 1 个单词，进入 WordBook 页面，系统返回回到“我的”，数量保留。
背诵进度：PASS，页面显示已有 `1 / 387` 进度；未清除数据，未破坏现有状态。
随机进度：部分验证，随机入口可操作且未见越界；未清除数据做独立随机序列重启断言。

### UI
360dp：未完全执行，当前连接设备为 1080x2374，未进行设备尺寸 override。
深色模式：PASS，系统夜间模式切换后页面可加载，文本与导航节点正常。
软键盘：部分验证，英文输入和提交可触发；adb 对清空/中文输入的行为受设备输入法影响。
Bottom Navigation：PASS，首页、单词、背诵、拼写、我的均可切换，单词本页返回链路正常。

### 问题
1. adb 原生输入中文及 Ctrl+A/删除在当前输入法上不稳定，导致中文搜索和清空搜索未完成可靠自动断言；严重程度：低（验收工具限制）；是否修复：未修改代码。
2. 当前设备不是 360dp，未做窄屏实机断言；严重程度：低（设备覆盖限制）；是否修复：未修改代码。
3. 验收过程中未清除应用数据，随机进度使用现有持久化状态，未做独立干净数据迁移验证；严重程度：低（按验收要求保留持久化）；是否修复：不适用。

### 代码变更
本阶段未修改业务代码，仅追加本日志记录。未修改 Repository、Room、JSON、Navigation、Gradle 或依赖。

- 2026-09-19：阶段 10.2 完成真题首页入口、真题列表、真题详情和专项占位页。首页新增“真题训练 / CET-6 历年真题”入口；列表和详情均从 `ExamRepository` 获取数据，动态显示试卷标题、57 题及写作 1/听力 25/阅读 30/翻译 1；整套与四个专项进入明确占位页，显示“该功能将在下一阶段实现”，未实现答题、音频、答案、进度、错题或成绩。新增页面复用 Compose Material 3、现有状态组件和 edge-to-edge 配置，未新增底部 Tab，未修改 Room、JSON、词汇、学习记录或单词本。真机验证首页→列表→详情→听力占位页及逐级返回通过，主 APK 启动通过；最终自定义 instrumentation 返回 `Exam data and Room persistence verified`。Gradle `assembleDebug`、`assembleDebugAndroidTest` 和 `connectedDebugAndroidTest` 成功；后者因项目使用自定义 instrumentation 入口显示 0 个 JUnit，已直接执行入口确认断言实际通过。一次直接 instrumentation 失败原因为重装测试 APK 后目标主 APK 暂未就绪，重新安装主 APK 和测试 APK 后通过。
### Warning
- 2026-09-19：阶段 10.3 完成阅读专项做题流程。新增 ReadingPracticeScreen 与 ReadingResultScreen，阅读入口从详情页直接进入，其他写作/听力/翻译/整套练习仍保持占位页。阅读题目来自 ExamRepository 的真实 JSON；答案仅保存在 Compose 内存 MutableMap<questionId, selectedAnswer>，不写 Room、SharedPreferences 或 DataStore。Section A 使用真实词库作为选项，Section B 使用真实 A-P 段落作为选项，Section C 根据 passageId 显示对应文章并使用题目 A-D 选项；未即时判题、不显示正确答案、解析、正确率或错题。支持选择修改、上一题/下一题、未作答确认、完成结果、重新开始清空答案和返回详情。构建 assembleDebug、assembleDebugAndroidTest、安装 APK/AndroidTest 均成功；真机自定义 instrumentation 返回 Exam data and Room persistence verified。首次编译发现并修复了导航补丁中的字符串转义和 Section A/B 选项来源问题；Windows apply_patch 包装器仍需通过临时复制的 runner 调用。未修改 JSON、Room、AppDatabase、migration、词汇模块或底部导航。由于当前环境没有视觉自动化通道，未虚构逐页人工点击结果，建议后续在设备上按首页→真题→阅读→第26题→第55题→结果完整点验。
本阶段未新增 warning。
原有 warning：`android.overridePathCheck` 实验性配置、AGP 8.5.2 与 compileSdk 35 兼容性提示；本次构建未重新产生 KAPT warning（任务为 up-to-date）。
- 2026-09-18：按用户确认，将上传的 CET6 图作为唯一视觉来源接入 Android Launcher Icon。新增 app/src/main/res/mipmap-mdpi、mipmap-hdpi、mipmap-xhdpi、mipmap-xxhdpi、mipmap-xxxhdpi 下的 ic_launcher.png 与 ic_launcher_round.png；新增 mipmap-anydpi-v26/ic_launcher.xml、ic_launcher_round.xml、drawable-nodpi/ic_launcher_foreground.png 与 values/colors.xml；AndroidManifest.xml 增加 android:icon 和 android:roundIcon。图片保持 1536x1536 原比例，Adaptive Icon 前景使用 78% 安全区，避免桌面裁剪 CET6 和卡片主体。首次 assembleDebug 因 120 秒工具超时未返回结论，续跑后 BUILD SUCCESSFUL，APK 为 app/build/outputs/apk/debug/app-debug.apk；仅有既有 AGP/compileSdk 与 overridePathCheck 警告，未修改其他业务代码。
- 2026-09-19：最终真机验证使用 PKB110（Android 16，YPSCWCZ5HMX4NN9X），最新 app-debug.apk 安装成功并可启动。顺序模式从第 1 推进至第 5，返回首页、完全重启后均恢复第 5；快速连续点击后重新进入显示第 6，与离开前一致。随机模式可独立从第 1 推进至第 3，切回顺序显示其独立进度；但完全退出后重新进入拼写页面默认进入顺序模式，重新切换随机显示第 1，未能证明随机进度跨 App 重启恢复，按失败报告。未修改功能代码，仅追加本日志。
- 2026-09-19：完成真题 JSON 数据审计。目录 `真题json文件` 下仅发现 `2025-12/cet6_2025_12_set1.json`（43064 bytes），无非 JSON 文件。文件可正常解析，UTF-8 无 BOM、无 NUL 字节、无重复 JSON 键；全局 ID 无重复，各题组题号连续且答案均落在对应选项/材料范围内，`answerKey` 与题目内 `correctAnswer` 一致。写作 1 题、听力 25 题、阅读 30 题（26--55）、翻译 1 题，共 57 题。唯一实际数据缺口：听力 1--25 的 `question` 字段全部为空，且未包含音频/媒体引用；若应用需展示听力题干或播放原音，需补充题干和媒体资源字段。阅读材料、选项文本、词库及段落均非空。未修改 JSON、Android 代码或依赖，仅追加本日志。
- 2026-09-19：阶段 10.1 真题数据层完成。新增 `Exam`、`ExamSection`、`ExamQuestion`、阅读材料模型、`ExamJsonParser` 和 `ExamRepository`；沿用 `org.json`，通过 Gradle 将根目录 `真题json文件` 作为单一 assets 源，APK 内路径为 `assets/2025-12/cet6_2025_12_set1.json`，源文件与合并 assets SHA-256 一致。听力 `question` 保留为空字符串，不视为异常；模型允许 nullable，并预留可选音频引用，不伪造音频。未修改 Room、AppDatabase、migration、导航、页面、词汇 JSON 或现有业务逻辑。现有 instrumentation 增加真题加载、57 题数量、四部分数量、阅读 26--55、题目 ID 唯一、answerKey 一致、听力空题干、阅读材料/选项、坏 JSON 明确报错等断言；设备执行返回 `Exam data and Room persistence verified`。Windows 直接运行 apply_patch 曾遇到 Access denied，按既有日志复制 patch runner 到临时目录后提交成功。

- 2026-09-19：仅替换 Android Launcher Icon。使用用户提供的 1254×1254 小毛蛋图片生成 ic_launcher 与 ic_launcher_round 的 mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi PNG，并以等比例缩放和安全留白生成 drawable-nodpi/ic_launcher_foreground.png；Adaptive Icon XML 保持引用 @color/ic_launcher_background 与 @drawable/ic_launcher_foreground，背景色更新为与源图一致的淡蓝色 #C9ECF8。未修改 AndroidManifest、app label、app_name、业务逻辑、页面、数据库、词库、Navigation、ViewModel 或 Repository。gradlew.bat assembleDebug --no-daemon --max-workers=1 BUILD SUCCESSFUL；仅出现既有 android.overridePathCheck 与 AGP/compileSdk 35 兼容性提示。

- 2026-09-20：CET6 词库由 387 扩充到 625。用户提供的 `词汇json文件/CET6_高频词汇_388-625.json` 为合法 UTF-8 JSON 数组，238 条记录，字段严格为 `id`、`word`、`phonetic`、`partOfSpeech`、`meaning`，ID 388--625 连续且无重复；复制到 `app/src/main/assets/cet6/CET6_高频词汇_388-625.json` 后，全量 5 个 JSON 共 625 条，ID 1--625 连续、重复 0、缺失 0、空 word 0、字段异常 0。`WordRepository` 原有自动枚举 `assets/cet6/*.json` 的机制未修改；`StudyProgressSupport` 已按 `words.size` 恢复顺序进度，并为随机序列保留有效旧 ID 后追加新增 ID，未修改 LearningRecord、WordBook、Room version 或 Migration。源码中发现的 `387` 仅有两个 Preview 示例和持久化兼容测试：Preview 已改为小型示例集；测试仍保留保存/恢复 `387` 索引的兼容断言。为本次词库覆盖，在既有自定义 `PersistenceInstrumentation` 加入 625 总数、连续 ID、无空字段、新增 ID 388/625、英文 `minimum` 与中文 `最低的` 搜索断言。`assembleDebug --no-daemon --max-workers=1 --offline --console=plain` BUILD SUCCESSFUL；自定义 instrumentation 在真机 `YPSCWCZ5HMX4NN9X` 直接执行返回 `Exam data and Room persistence verified`。最新 APK 安装成功；真机 UI dump 已确认首页显示 `/ 625` 和 `625`、单词页显示“全部 625 个词”，并可英文搜索新增 `minimum /ˈmɪnɪməm/ adj. 最低的`。本轮自动化未稳定完成背诵页、拼写页、单词本的完整真机交互及中文 UI 输入验收，不能将这些项目报为 PASS。工具问题：Windows 当前 `apply_patch` 返回 Access denied，按既有问题采用受限 `.NET` 文本替换；PowerShell `ConvertFrom-Json` 管道会把整个数组作为一个对象，必须通过显式 `foreach` 展平后再统计，否则会产生总数/ID 异常的假报错。
- 2026-09-20：完成 1--625 词库扩充后的最终新增词回归测试，未修改业务代码、JSON、Room 或数据库数据。设备 `YPSCWCZ5HMX4NN9X` 在线并安装既有 APK。单词页 PASS：显示“全部 625 个词”；搜索 `minimum`（ID 388）显示 `/ˈmɪnɪməm/`、`adj.`、`最低的`；搜索 `routine`（ID 625）显示 `adj.; n.`、`常规的；常规`。普通顺序背诵 PASS：恢复 `3 / 625`，推进到 `388 / 625` 显示 `NO.388 minimum`，继续到 `625 / 625` 显示 `NO.625 routine`，上一词/下一词可用且无崩溃。普通随机背诵 PASS（本次会话）：显示 `1 / 625` 的随机 ID 341，下一词为 `2 / 625` 的随机 ID 382；重新进入页面默认回到顺序模式并恢复 `625 / 625`。随机模式跨退出后自动恢复未执行，因为页面重新进入默认顺序且本轮未稳定完成再次切换随机后的持久化对照。拼写 PASS（访问与边界）：显示 `第 1 / 625 词`，推进到 `第 388 / 625 词`，输入并提交 `minimum` 后进入下一词状态，显示答案，无数组越界或崩溃；正确/错误具体反馈文案未被 UI dump 稳定捕获，反馈细节未执行。我的单词本部分：加入新增 ID 625 `routine` PASS，按钮变为“移出单词本”；我的页面显示“我的单词本 / 1 个单词”；单词本显示 `NO.625 routine` 并可进入开始背诵，单词本背诵页面显示 `routine` PASS。移除后数量恢复未执行：返回操作受到系统 Launcher/照片浮层干扰，无法稳定回到可点击移除状态。LearningRecord 隔离：提交动作和拼写页面状态变化已执行，但返回我的页面时仍显示 `0 / 625`、待学习 `625`，未观察到对应统计增加，因此按 FAIL/未达预期记录，不推测为 PASS；需后续在稳定页面状态下复核，但本轮不修改代码。数据兼容：未清除数据库；首页/我的页面显示总数 625、待学习 625；未观察到旧数据被清除；新增词单词本记录由用户实际点击产生。生产代码扫描未发现总词数固定 625，页面继续使用 `words.size`/动态统计；源码中的 `387` 仅保留在兼容测试，`625` 仅出现在词库完整性测试或动态结果中。受限项：系统 Launcher/照片浮层、输入焦点和 UI 自动化清空输入不稳定，导致单词本移除、拼写反馈文案、随机模式跨退出恢复、LearningRecord 统计变化等未能全部完成稳定验证。- 2026-09-20：修复拼写进度时序 Bug，仅修改 `app/src/main/java/com/example/cet6vocabulary/presentation/screens/SpellingScreen.kt`。根因是初始化期间 `currentIndex=0` 与生命周期兜底保存存在覆盖风险；保存任务虽有 Mutex，但旧快照等待后仍可能晚于新快照写入；DisposableEffect 可能捕获旧状态；模式切换保存还可能在 `randomWords` 重组前看到空列表。修复为 `LaunchedEffect(Unit)` 一次性加载、`restored` 门闸、按 progressKey 保存最新快照并仅允许当前快照落库、生命周期使用 `rememberUpdatedState` 的最新快照、模式切换读取各自 Room 进度并使用已构造的随机词列表保存；完成索引仍允许 `0..words.size`。未修改背诵功能、Room 结构或公共背诵逻辑。`assembleDebug`、`assembleDebugAndroidTest` 均通过；真机 PKB110 安装成功；PersistenceInstrumentation 返回 `Exam data and Room persistence verified`；真机验证“从头开始”回到第1个、继续到第2个、完全重启后恢复第2个均通过。期间一次受限文本替换误写字面量 `\n` 导致短暂编译失败，已修复并最终构建通过。
- 2026-09-21：第二次词库扩充接入 626--781。用户提供的 `词汇json文件/CET6_高频词汇_626-781.json` 为合法 JSON 数组，156 条记录，ID 626--781 连续、无重复，字段严格包含 `id`、`word`、`phonetic`、`partOfSpeech`、`meaning`，无 day/每日目标等额外字段；首词为 fiscal，末词为 subordinate。文件已复制为 `app/src/main/assets/cet6/CET6_高频词汇_626-781.json`，源文件与 assets SHA-256 一致。合并后的真实资产数量为 6 个 JSON（1--73、74--150、151--229、230--387、388--625、626--781；用户描述“共5个JSON”与六个区间不一致，未合并或重命名旧文件），共 781 条，ID 1--781 连续，重复 0、缺失 0、空 word 0、必需字段缺失 0，1--625 全部保留；旧五个词库文件 SHA-256 均未变化。WordRepository 仍自动枚举 `assets/cet6` 下所有 `.json`、合并后按 ID 排序，本次未修改；生产页面继续使用 `words.size`/动态统计，无固定 625/781 总数；AppDatabase 仍 version 3，无新增 Migration，StudyProgress、LearningRecord、WordBook、Spelling 业务代码未修改。仅更新 `app/src/androidTest/java/com/example/cet6vocabulary/data/LearningRecordPersistenceTest.kt` 的词库完整性断言，从 625/minimum 更新为 781/fiscal/subordinate，并增加真实新增词搜索断言。`assembleDebug assembleDebugAndroidTest --no-daemon --max-workers=1 --offline --console=plain` BUILD SUCCESSFUL；独立 `assembleDebug` 亦 BUILD SUCCESSFUL。APK 已确认包含 `assets/cet6/CET6_高频词汇_626-781.json`。真机设备 `YPSCWCZ5HMX4NN9X` 在本轮 ADB 重启后仍未出现在设备列表，故未执行安装、真机启动和自定义 PersistenceInstrumentation，不能报告为 PASS。环境问题：ADB daemon 重启后设备未连接；PowerShell `Test-Path $source -and Test-Path $asset` 语法写法错误，已改为括号包裹条件；APK 内中文文件名用 `jar tf` 输出乱码，但资源路径已确认存在。
- 2026-09-21：按用户要求开始 CET6 626--781 真机补测，首先执行 `adb devices -l`；设备 `YPSCWCZ5HMX4NN9X` 未出现在列表中，ADB 状态不是 `device`。严格按要求停止后续操作，未安装 APK、未启动 App、未执行 `pm clear`/卸载、未修改任何业务代码、JSON、Room、StudyProgress、LearningRecord 或 WordBook；首页、单词、背诵、拼写、单词本、LearningRecord 和 StudyProgress 真机补测全部未执行。当前 APK 文件仍为 `app/build/outputs/apk/debug/app-debug.apk`。
- 2026-09-21：用户表示设备已重新连接后再次开始 626--781 真机补测，但当前执行环境重新运行 `adb devices -l` 仍为空，`adb -s YPSCWCZ5HMX4NN9X get-state` 返回 `device not found`。按要求立即停止，未安装 APK、未启动 App、未执行 `pm clear` 或卸载，未修改任何代码、JSON、Room、StudyProgress、LearningRecord 或 WordBook；所有本轮真机补测项目继续未执行。

- 2026-09-22：完成 GitHub 上传前本地整理预检查，未执行 git add、git commit 或 git push，未修改业务代码。确认 .gitignore 保留 app/src/main/assets/cet6/ 下 6 个公开词汇 JSON，同时排除 .gradle/、.idea/、**/build/、APK/AAB、.hprof、local.properties、签名文件、根目录 词汇json文件/ 和整个 真题json文件/。README 已覆盖项目功能、技术栈、结构、构建与资料版权说明。敏感信息扫描未发现 API Key、Token、密码、私钥或签名文件；扫描命中的 secret 仅为词汇数据中的普通单词。

- 2026-09-22：GitHub 首次提交前完成本仓库提交者身份配置：user.name 为 Lee，user.email 为 501597820@qq.com（仅仓库级配置，未写入密码、Token 或其他敏感信息）。已核验 65 个暂存文件不含真题资料、构建产物、密钥或本地配置；执行首次提交，未配置 remote、未执行 push 或 pull。
